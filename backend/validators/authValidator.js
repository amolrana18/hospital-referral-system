// Auth validators
const authValidators = {
  register: [
    // Add validation rules here
  ],
  login: [
    // Add validation rules here
  ]
};

// Hospital validators
const hospitalValidators = {
  create: [
    // Add validation rules here
  ],
  update: [
    // Add validation rules here
  ]
};

// Referral validators
const referralValidators = {
  create: [
    // Add validation rules here
  ],
  update: [
    // Add validation rules here
  ]
};

module.exports = {
  authValidators,
  hospitalValidators,
  referralValidators
};